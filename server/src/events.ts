export enum Event {
  CONNECT = "connect",
  DISCONNECT = "disconnection",
  REGISTER = "register",
  LOGIN = "login",
  JOIN = "join",
}

export enum ErrorEvents {
  REGISTER = "registerError",
  LOGIN = "loginError",
  JOIN = "joinError",
}
